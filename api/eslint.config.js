// Importa el módulo 'neostandard' y lo ejecuta como función.
// 'neostandard' es probablemente una configuración personalizada de ESLint o similar.
// El objeto vacío {} representa las opciones de configuración (ninguna en este caso).
// El resultado de esa función se exporta como módulo, para que pueda ser usado en otros archivos.

module.exports = require('neostandard')({
  // Puedes agregar opciones aquí si necesitas personalizar la configuración
})
